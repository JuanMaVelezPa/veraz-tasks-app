spring.application.name=TasksBackend

# Server configuration
server.address=localhost
server.servlet.context-path=/api                
server.port=3000

# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/tasks_app_db
spring.datasource.username=postgres
spring.datasource.password=jmvelez
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# JWT configuration - SECURITY: Use environment variables in production!
veraz.app.jwtSecret=${JWT_SECRET:V3r4zT4sks@AppP@sS@pP@sS@w0Rd1o10sS}
veraz.app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}

# Logging configuration - Optimized for production
logging.level.root=INFO
logging.level.com.veraz.tasks.backend=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.file.name=./logs/tasks-backend.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n