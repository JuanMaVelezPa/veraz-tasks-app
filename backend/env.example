# ===========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ===========================================
# 
JWT_SECRET=your-super-secure-jwt-secret-here-min-32-characters
JWT_EXPIRATION_MS=86400000

# ===========================================
# INSTRUCCIONES DE USO
# ===========================================
# 1. Copia este archivo como .env
# 2. Reemplaza los valores con tus configuraciones reales
# 3. NUNCA commits el archivo .env al repositorio
# 4. En producción, usa variables de entorno del sistema o un gestor de secretos

# ===========================================
# COMANDOS PARA GENERAR SECRETOS SEGUROS
# ===========================================
# Opción 1: OpenSSL
# openssl rand -base64 32

# Opción 2: Node.js
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Opción 3: Python
# python3 -c "import secrets; print(secrets.token_urlsafe(32))" 